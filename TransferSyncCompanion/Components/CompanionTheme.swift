import SwiftUI

// Numeric sizing constants. Colors live in Color+Companion, fonts in Font+Companion.
enum CompanionTheme {
    static let cardCornerRadius: CGFloat = 12
    static let pillCornerRadius: CGFloat = 8
    static let statusIconSize: CGFloat = 18

    static let ctaBackground: Color = .white
    static let ctaForeground: Color = .black
    static let ctaHeight: CGFloat = 44
    static let ctaHorizontalPadding: CGFloat = 10

    static let ghostCornerRadius: CGFloat = 8
    static let ghostIconSize: CGFloat = 28
}
